module.exports = app => {
    //if (app.config.env === 'local') {
    app.beforeStart(async () => {
        // await app.model.sync({ force: true });
        // await app.model.Consumption.sync({ force: true });
        // await app.model.Consumption.sync({ force: true });
        // console.log('abc');
    });
    //}

};
